# 跨进程引用计数同步方案设计

## 当前问题

**问题**：100个并发agent进程，需要跨进程同步引用计数，但当前实现中：
- 引用计数只在本地进程维护（`_ref_counts`字典）
- 注册表中没有存储引用计数
- `max_concurrency_per_sandbox`限制无法跨进程生效

**影响**：
- 进程A认为ref_count=2，进程B认为ref_count=1
- 实际总引用=3，但每个进程都认为没超限
- 如果max_concurrency_per_sandbox=2，应该拒绝，但无法检测

## 方案对比

### 方案1：在注册表中存储ref_count（文件锁保护）

**实现**：
- 在注册表JSON中添加`ref_count`字段
- 每次acquire/release时，使用文件锁保护，读取-修改-写入

**优点**：
- 实现简单，不需要额外依赖
- 可以准确跟踪跨进程引用计数

**缺点**：
- 文件锁可能成为性能瓶颈（100个进程竞争）
- 每次acquire/release都需要文件I/O和锁操作
- 锁竞争可能导致延迟

**性能估算**：
- 文件锁操作：~1-5ms
- 100个进程并发：可能产生锁竞争，延迟增加
- 如果每个进程每秒10次操作：1000次/秒，文件锁可能成为瓶颈

### 方案2：使用Redis等外部存储

**实现**：
- 使用Redis存储引用计数（原子操作）
- acquire时：`INCR sandbox:{sandbox_id}:ref_count`
- release时：`DECR sandbox:{sandbox_id}:ref_count`

**优点**：
- 性能好，Redis原子操作很快
- 支持高并发（Redis可以处理10万+ QPS）
- 实现简单

**缺点**：
- 需要额外依赖（Redis）
- 需要维护Redis服务
- 增加系统复杂度

### 方案3：乐观锁+重试机制（推荐）

**实现**：
- 在注册表中存储ref_count
- 使用版本号（version）或时间戳作为乐观锁
- 读取-修改-写入时检查版本号，如果冲突则重试

**优点**：
- 不需要阻塞锁，性能较好
- 实现相对简单
- 适合读多写少的场景

**缺点**：
- 高并发时可能重试次数多
- 需要处理重试逻辑

### 方案4：分离式设计（推荐用于高并发）

**实现**：
- 注册表只存储sandbox元数据（不存储ref_count）
- 使用独立的引用计数文件：`{tag}_{sandbox_id}.ref_count`
- 使用原子文件操作（如`os.rename`）或简单的计数器文件

**优点**：
- 减少锁竞争（每个sandbox独立文件）
- 可以批量更新
- 性能较好

**缺点**：
- 实现稍复杂
- 需要处理文件清理

### 方案5：混合方案（最佳平衡）

**实现**：
- 注册表中存储ref_count，但使用**非阻塞锁+缓存+批量更新**
- 本地维护一个"待更新队列"
- 定期批量更新注册表（减少锁竞争）
- acquire/release时先更新本地，异步更新注册表

**优点**：
- 性能好（减少文件I/O）
- 正确性有保障（最终一致性）
- 适合高并发场景

**缺点**：
- 实现较复杂
- 可能有短暂的不一致（但可接受）

## 我的推荐方案

### 推荐：方案5（混合方案）+ 方案3（乐观锁）

**核心思路**：
1. **注册表中存储ref_count**，使用乐观锁机制
2. **本地缓存+异步更新**：acquire/release时先更新本地，异步更新注册表
3. **批量更新**：定期批量同步，减少文件I/O
4. **非阻塞更新**：如果锁被占用，跳过本次更新（最终一致性）

**性能优化**：
- 减少文件锁竞争：使用非阻塞锁
- 减少I/O：批量更新，异步更新
- 提高并发：每个sandbox独立文件，减少锁范围

**实现细节**：

```python
# 注册表格式（增强版）
{
    "sandbox_id": "...",
    "tag": "...",
    "container_id": "...",
    "created_at": 1234567890,
    "last_used": 1234567890,
    "ref_count": 3,        # 新增：跨进程引用计数
    "version": 5           # 新增：版本号（乐观锁）
}

# acquire_sandbox时：
1. 读取注册表，获取ref_count和version
2. 检查是否超过max_concurrency_per_sandbox
3. 如果通过，ref_count++，version++，写入注册表（乐观锁）
4. 如果冲突（version不匹配），重试或创建新的

# release_sandbox时：
1. 本地ref_count--
2. 异步更新注册表：ref_count--，version++
3. 如果锁被占用，跳过（最终一致性）
```

## 性能评估

**100个并发进程场景**：

| 方案 | 每次操作延迟 | 吞吐量 | 复杂度 |
|------|------------|--------|--------|
| 方案1（文件锁） | 5-20ms | 低（锁竞争） | 低 |
| 方案2（Redis） | 0.1-1ms | 高（10万+ QPS） | 中 |
| 方案3（乐观锁） | 2-10ms | 中高 | 中 |
| 方案4（分离文件） | 1-5ms | 中高 | 中 |
| 方案5（混合） | 0.1-2ms | 高 | 高 |

**推荐**：方案5（混合方案），因为：
- 性能好（本地缓存+异步更新）
- 不需要额外依赖
- 适合100个并发进程
- 最终一致性可接受

## 实现建议

1. **阶段1**：先实现方案1（简单版本），验证功能
2. **阶段2**：优化为方案5（混合方案），提升性能
3. **阶段3**：如果性能仍不够，考虑方案2（Redis）

需要我实现哪个方案？

